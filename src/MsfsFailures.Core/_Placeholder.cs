namespace MsfsFailures.Core;

// Placeholder so the assembly has at least one type.
// Domain (WearEngine, FailureEngine, MaintenanceService) lands here in a later pass — see PLAN.md.
internal static class AssemblyMarker;
